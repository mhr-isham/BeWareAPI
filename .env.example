# Example environment variables for BeWare API

# Secret key for signing JWT tokens
JWT_SECRET=your_jwt_secret_here

# Port to run the server on
PORT=3000

# SQLite database file path (default: beware.db)
DB_FILE=beware.db